import type {MetadataRoute} from 'next'; import {articles} from '@/lib/content'; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://court-report.example';return ['','/articles','/gallery','/club','/about','/contact','/newsletter',...articles.map(a=>`/articles/${a.slug}`)].map(url=>({url:`${base}${url}`,lastModified:new Date(),changeFrequency:'weekly',priority:url===''?1:.7}))}
