import Link from 'next/link'; import {articles,categories} from '@/lib/content'; export const metadata={title:'Stories'};
export default function Articles(){return <section className="section"><div className="shell"><p className="eyebrow">The journal</p><h1 className="display" style={{fontSize:'clamp(52px,7vw,82px)',margin:'9px 0 25px'}}>Stories from<br/><i>the good part.</i></h1><nav aria-label="Story categories" style={{display:'flex',gap:9,flexWrap:'wrap',marginBottom:36}}>{categories.map((c,i)=><span key={c} className="pill" style={{background:i===0?'#d5ef87':'#e9e4d8'}}>{c}</span>)}</nav><div className="grid" style={{gridTemplateColumns:'repeat(auto-fit,minmax(240px,1fr))'}}>{articles.map(a=><Link href={`/articles/${a.slug}`} key={a.slug} className="card" style={{padding:25,minHeight:235,display:'flex',flexDirection:'column',justifyContent:'end'}}><span className="eyebrow">{a.category} · {a.date}</span><h2 className="display" style={{fontSize:35,lineHeight:1,margin:'12px 0'}}>{a.title}</h2><p style={{fontSize:14,lineHeight:1.5,color:'#59697a'}}>{a.excerpt}</p></Link>)}</div></div></section>}
